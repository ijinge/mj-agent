# mj-agent 基础运行依赖（worker + gateway 都依赖）
# 由 pip install -r requirements.txt 装入 .venv

# Web 框架
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0

# 异步运行时与生态
redis>=5.0,<6.0
SQLAlchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
PyYAML>=6.0,<7.0

# Agent / LangGraph
langgraph>=0.2.34,<1.0
langchain-core>=0.3.7,<1.0
langchain-openai>=0.2.0,<1.0
langchain-anthropic>=0.2.0,<1.0

# MCP（Model Context Protocol）— 接入外部 tools
mcp>=1.0.0
langchain-mcp-adapters>=0.1.0
